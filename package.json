{
  "name": "openai-chatterbox",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "nuxt dev --hostname '0' --port=5000",
    "build": "nuxt build",
    "dev": "nuxt dev --hostname '0' --port=5000",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {},
  "dependencies": {
    "formidable": "^2.1.1",
    "nuxt": "^3.1.0"
  },
  "engines": {
    "node": "18.x"
  }
}
